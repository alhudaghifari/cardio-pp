<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/redkuat</item>
        <item name="colorControlHighlight">@color/silver</item>
    </style>

    <style name="CustomActionBarTransparant" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>

    <style name="TextAppearence.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/redkuat</item>
        <item name="android:colorBackground">@color/redkuat</item>
        <item name="android:colorActivatedHighlight">@color/redkuat</item>
        <item name="android:colorPrimary">@color/redkuat</item>
        <item name="android:colorEdgeEffect">@color/redkuat</item>
    </style>

</resources>
